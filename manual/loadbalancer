#loadbalancer, welcher mittels HAProxy realisiert wird.

#1. Installiere HAProxy
sudo yum install -y haproxy
sudo apt-get install -y haproxy

#2. Konfiguration einstellen
sudo cp haproxy.conf /etc/haproxy/

#3 (Ubuntu): /etc/default/haproxy
Set ENABLED=1

#3. HAProxy staren und aktivieren
sudo service haproxy start



